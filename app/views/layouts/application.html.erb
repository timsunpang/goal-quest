<!DOCTYPE html>
<html>
<head>
  <title>GoalQuest</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= csrf_meta_tags %>
  <link href='https://fonts.googleapis.com/css?family=Archivo+Narrow:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
</head>
<body>

<%= yield %>

<% if flash %>
  <% flash.each do |category, msg| %>
    <%= msg.join('<br>').html_safe%><br>
  <% end %>
<% end %>

<% if signed_in? %>
  Welcome, <%= current_user.username %>
<% end %>



<% if signed_in? %>
  <%= button_to "Log Out", session_url, method: :delete%>
<% else %>
  <%= link_to "Or sign up as a new user!", new_api_user_url unless current_page?(new_api_user_url) %><br>
  <%= link_to "Or take a guest tour!", new_session_url unless current_page?(new_api_user_url) %>

<% end %>

</body>
</html>
